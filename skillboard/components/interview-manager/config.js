import { DbEngine } from '../../shared/db-engine.js';
import { InterviewManagerConfig } from '../../config/config-interview-manager.js';
export const config = InterviewManagerConfig;
export { DbEngine };
