import { DbEngine } from '../../shared/db-engine.js';
import { CookieAccountingConfig } from '../../config/config-cookie-accounting.js';
export const config = CookieAccountingConfig;
export { DbEngine };
